import { PublicPlaceholder } from "@/features/marketing/PublicPlaceholder";
export default function Page() {
  return (
    <PublicPlaceholder
      eyebrow="Outbound automation"
      title="Human-controlled outreach is the planned launch direction."
      description="The current application prepares grounded drafts and exports them for review. Connected mailboxes, sending, sequences, follow-ups, and reply handling are planned and are not yet available."
      note="Planned capability — this placeholder deliberately does not claim that automated sending is implemented."
    />
  );
}
