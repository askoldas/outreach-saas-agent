import { PublicPlaceholder } from "@/features/marketing/PublicPlaceholder";
export default function Page() {
  return (
    <PublicPlaceholder
      eyebrow="Security"
      title="Workspace isolation and source transparency by design."
      description="Opptium uses authenticated workspace boundaries, PostgreSQL row-level security, server-only provider credentials, immutable grounding references, and human review."
    />
  );
}
