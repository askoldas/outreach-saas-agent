import { PageHeader } from "@/components/ui/PageHeader";

export default function NewCampaignPage() {
  return (
    <PageHeader
      title="Create campaign"
      description="The campaign wizard will be added in the campaign interface batch."
    />
  );
}
