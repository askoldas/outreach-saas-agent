import { PageHeader } from "@/components/ui/PageHeader";

export default function CampaignsPage() {
  return (
    <PageHeader
      title="Campaigns"
      description="Prospecting campaigns and strategy review states will appear here in the prototype."
    />
  );
}
